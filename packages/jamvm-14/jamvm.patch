--- a/src/class.c
+++ b/src/class.c
@@ -601,7 +601,7 @@ Class *createArrayClass(char *classname, Object *class_loader) {
         classblock->dim = CLASS_CB(comp_class)->dim + 1;
     } else { 
         if(classname[1] == 'L') {
-            char element_name[len-2];
+            char element_name[len-2+1];
 
             strcpy(element_name, classname + 2);
             element_name[len-3] = '\0';
@@ -1653,9 +1653,9 @@ char *getClassPath() {
     return classpath;
 }
 
-int filter(struct dirent *entry) {
+int filter(const struct dirent *entry) {
     int len = strlen(entry->d_name);
-    char *ext = &entry->d_name[len-4];
+    const char *ext = &entry->d_name[len-4];
 
     return len >= 4 && (strcasecmp(ext, ".zip") == 0 ||
                         strcasecmp(ext, ".jar") == 0);
--- a/src/os/linux/os.c
+++ b/src/os/linux/os.c
@@ -19,6 +19,7 @@
  * Foundation, 51 Franklin Street, Fifth Floor, Boston, MA 02110-1301, USA.
  */
 
+#define _GNU_SOURCE
 #include <stdio.h>
 #include <string.h>
 #include <stdlib.h>
