--- a/native/fdlibm/dtoa.c
+++ b/native/fdlibm/dtoa.c
@@ -25,6 +25,7 @@
 	U.S.A.
 	dmg@research.att.com or research!dmg
  */
+#include <stdlib.h>
 
 #include "mprec.h"
 #include <string.h>
--- a/native/jni/classpath/native_state.c
+++ b/native/jni/classpath/native_state.c
@@ -59,8 +59,7 @@ cp_gtk_init_state_table_with_size (JNIEnv * env, jclass clazz, jint size)
 
   table = (struct state_table *) malloc (sizeof (struct state_table));
   table->size = size;
-  table->head = (struct state_node **) calloc (sizeof (struct state_node *),
-					       table->size);
+  table->head = (struct state_node **) calloc (table->size, sizeof (struct state_node *));
   table->hash = hash;
   table->clazz = clazz_g;
 
--- a/native/jni/java-lang/java_lang_VMProcess.c
+++ b/native/jni/java-lang/java_lang_VMProcess.c
@@ -209,7 +209,7 @@ Java_java_lang_VMProcess_nativeSpawn (JNIEnv * env, jobject this,
   err = cpproc_forkAndExec(strings, newEnviron, fds, pipe_count, &pid, dir);
   if (err != 0)
     {
-      strncpy(errbuf, cpnative_getErrorString (err), sizeof(errbuf));
+      snprintf (errbuf, sizeof(errbuf), "%s", cpnative_getErrorString (err));
       goto system_error;
     }
 
--- a/native/jni/native-lib/cpio.c
+++ b/native/jni/native-lib/cpio.c
@@ -368,6 +368,8 @@ int cpio_checkType (const char *filename, jint *entryType)
 {
   struct stat statbuf;
 
+  *entryType = CPFILE_UNKNOWN;
+
   if (stat(filename, &statbuf) < 0)
     return errno;
 
@@ -450,16 +452,9 @@ int cpio_closeDir (void *handle)
 
 int cpio_readDir (void *handle, char *filename)
 {
-#ifdef HAVE_READDIR_R
-  struct dirent dent;
-#endif /* HAVE_READDIR_R */
   struct dirent *dBuf;
 
-#ifdef HAVE_READDIR_R
-  readdir_r ((DIR *) handle, &dent, &dBuf);
-#else
   dBuf = readdir((DIR *)handle);
-#endif /* HAVE_READDIR_R */
 
   if (dBuf == NULL)
     {
--- a/native/jni/native-lib/cpio.h
+++ b/native/jni/native-lib/cpio.h
@@ -68,6 +68,7 @@ JNIEXPORT int cpio_closeOnExec(int fd);
 
 #define CPFILE_FILE 0
 #define CPFILE_DIRECTORY 1
+#define CPFILE_UNKNOWN 2
 
 JNIEXPORT int cpio_setFileReadonly (const char *filename);
 JNIEXPORT int cpio_isFileExists (const char *filename);
--- a/native/jni/native-lib/cpproc.c
+++ b/native/jni/native-lib/cpproc.c
@@ -86,7 +86,10 @@ int cpproc_forkAndExec (char * const *commandLine, char * const * newEnviron,
 
       close_all_fds(local_fds, pipe_count * 2);
 
-      chdir(wd);
+      {
+        int err = chdir(wd);
+        if (err) return err;
+      }
       if (newEnviron == NULL)
 	execvp(commandLine[0], commandLine);
       else
--- a/vm/reference/java/lang/VMProcess.java
+++ b/vm/reference/java/lang/VMProcess.java
@@ -200,6 +200,7 @@ final class VMProcess extends Process
 	{
 	  try
 	    {
+            System.err.println("native spawning");
 	      process.nativeSpawn(process.cmd, process.env, process.dir,
 				  process.redirect);
 	      process.state = RUNNING;
@@ -234,15 +235,19 @@ final class VMProcess extends Process
     // Add process to the new process work list and wakeup processThread
     synchronized (workList)
       {
+          System.err.println("Adding process to workList");
 	workList.add(this);
 	if (processThread == null)
 	  {
+          System.err.println("Started processThread");
 	    processThread = new ProcessThread();
 	    processThread.setDaemon(true);
 	    processThread.start();
 	  }
 	else
 	  {
+          System.err.println("Notified workList");
+
 	    workList.notify();
 	  }
       }
@@ -254,10 +259,12 @@ final class VMProcess extends Process
 	  {
 	    try
 	      {
+              System.out.println("Waiting for processThread");
 		wait();
 	      }
 	    catch (InterruptedException e)
 	      {
+              System.out.println("Interrupte");
 		/* ignore */
 	      }
 	  }
@@ -284,6 +291,7 @@ final class VMProcess extends Process
   private void setProcessInfo(OutputStream stdin,
 			      InputStream stdout, InputStream stderr, long pid)
   {
+      System.err.println("setProcessInfo | stdin: " + stdin + " stdout: " + stdout + " stderr: " + stderr + " pid: " + pid);
     this.stdin = stdin;
     this.stdout = stdout;
     if (stderr == null)
@@ -341,8 +349,11 @@ final class VMProcess extends Process
 
   public synchronized int waitFor() throws InterruptedException
   {
-    while (state != TERMINATED)
-      wait();
+      System.err.println("waitFor enter: " + state);
+    while (state != TERMINATED) {
+        wait();
+        System.err.println("waitFor after wait: " + state);
+    }
     return exitValue;
   }
 
